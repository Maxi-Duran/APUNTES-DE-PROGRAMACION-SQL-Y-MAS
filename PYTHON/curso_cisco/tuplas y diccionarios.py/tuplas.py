tuple_1 = (1, 2, 4, 8)
tuple_2 = 1., .5, .25, .125         #diferencia de una tupla y lista es que las tuplas usan parentesis 
                                    #a función len() acepta tuplas, y regresa el número de elementos contenidos dentro;
                                    #el operador + puede unir tuplas (ya se ha mostrado esto antes)
                                    #los operadores in y not in funcionan de la misma manera que en las listas.
